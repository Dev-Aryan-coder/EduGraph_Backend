spring.application.name=EduGraph

# ===== MySQL =====
spring.datasource.url=jdbc:mysql://localhost:3306/edugraph_db?createDatabaseIfNotExist=true
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:7474}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# ===== Email (SMTP) =====
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${MAIL_USERNAME:yonexffyt14@gmail.com}
spring.mail.password=${MAIL_PASSWORD:alaq wbyd tith ftmx}

spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true

# ===== Server =====
server.port=8080
# ===== JWT Configuration =====
app.jwt.secret=404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
app.jwt.expiration-ms=86400000

# ===== CORS Configuration (Permits all HTTP and HTTPS ports for React / Local dev) =====
app.cors.allowed-origin-patterns=http://localhost:*,https://localhost:*,http://127.0.0.1:*,https://127.0.0.1:*

# ===== Multipart / File Upload Limits (For Excel Bulk Import & Reference Files) =====
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB
app.upload.dir=src/main/resources/static/uploads
